# v1.0.0-beta.1 verification staging

This branch carries the byte-verified v1 overlay and Windows verification workflow. It must not publish or delete legacy releases while running as a pull request. Promotion to main is allowed only after the PR Windows gate succeeds.
